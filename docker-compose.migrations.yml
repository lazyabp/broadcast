version: '3.4'

services:
  migrations:
    build:
      context: ../../
      dockerfile: templates/service/database/Dockerfile
    depends_on:
      - sqlserver
    environment:
      - IdentityServer_DB=BroadcastKit_Identity
      - BroadcastKit_DB=BroadcastKit_ModuleDb
      - SA_PASSWORD=yourStrong(!)Password
